{
  "name": "mcp-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python mcp_server.py",
    "build": "cd frontend && npm run build",
    "prod": "cd frontend && npm run build && cd .. && python mcp_server.py"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
} 